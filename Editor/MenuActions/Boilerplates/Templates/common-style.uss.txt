:root {
    --anchor-size: 32px;
    --padding-size: 32px;
    --background-slice-width: 64;
    --minimum-size: 129px;
    --dialog-toolbar-offset: 16px;
}

VisualElement#Themes {
    display: flex;
    flex-direction: row-reverse;
}

VisualElement.left-toolbar {
    position: absolute;
    display: flex;
    flex-direction: row;
    left: 0;
    top: 0;
}

VisualElement.right-toolbar {
    position: absolute;
    display: flex;
    flex-direction: row-reverse;
    right: 0;
    top: 0;
}

VisualElement.dialog {
    position: absolute;
    display: none;
    padding: var(--padding-size);
    -unity-slice-left: var(--background-slice-width);
    -unity-slice-top: var(--background-slice-width);
    -unity-slice-right: var(--background-slice-width);
    -unity-slice-bottom: var(--background-slice-width);
    min-width: var(--minimum-size);
    min-height: var(--minimum-size);
}

VisualElement.dialog > VisualElement.left-toolbar {
    left: var(--dialog-toolbar-offset);
    top: var(--dialog-toolbar-offset);
}

VisualElement.dialog > VisualElement.right-toolbar {
    right: var(--dialog-toolbar-offset);
    top: var(--dialog-toolbar-offset);
}

VisualElement.dialog > VisualElement.left-toolbar > Button,
VisualElement.dialog > VisualElement.right-toolbar > Button {
    color: white;
    margin: 0;
    padding: 4px;
    width: 32px;
    height: 32px;
}

VisualElement.section {
    left: var(--anchor-size);
    top: var(--anchor-size);
    right: var(--anchor-size);
    bottom: var(--anchor-size);
}

VisualElement.message {
    left: 50%;
    top: 50%;
}

VisualElement.dialog.open {
    display: flex;
}

VisualElement.dialog > Label.title {
    font-size: 28px;
    -unity-text-align: middle-center;
}

VisualElement.dialog Button {
    color: white;
    border-radius: 4px;
    border-width: 2px;
}

VisualElement.dialog Button:disabled {
    color: gray;
}

VisualElement.dialog > VisualElement.content {
    flex-basis: 10px;
    flex-grow: 4;
}

VisualElement.dialog > VisualElement.buttons {
    margin-top: 10px;
    flex-direction: row;
    justify-content: space-around;
    flex-basis: 10px;
    flex-grow: 1;
}

VisualElement.dialog > VisualElement.buttons Button {
    align-self: stretch;
}
